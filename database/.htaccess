# Denegar acceso a archivos de base de datos
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
</IfModule>

# Deshabilitar listado de directorios
Options -Indexes
